Assignment Instructions:

Your program will in initially obtain a file name and, if not found, the user is informed, and the program exits.
Otherwise, the user is presented with a menu. The user chooses one of the options (see MenuChoices.txt) and
the application creates the appropriate data structure and executes the associated traversal mode function. It
reads Terms from the file into the selected container, sorts the Terms in descending order of exponents, and
outputs the polynomial represented by the terms, well-labeled in conventional form, obtains a value for the
variable, and outputs P(x
